{
  "name": "@bb-84c/dsh-mobile-cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "Mobile remote-access control plane for DeepSeek Harness (dsh). CLI business modules only; the dsh plugin entry (bundle patch + cmdline parsing) is wired separately.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test test/"
  },
  "dependencies": {
    "commander": "^15"
  }
}
